# FreshData Benchmark Suite — Dependencies
# ==========================================
# Core benchmark target
freshdata-cleaner[all]

# Comparison libraries
pandas>=2.0
polars>=0.20
pyjanitor>=0.27
feature-engine>=1.6
scikit-learn>=1.3

# Benchmark framework
asv>=0.6

# Data generation & measurement
numpy>=1.24
pyarrow>=10
psutil>=5.9

# Report generation
matplotlib>=3.7
tabulate>=0.9

# Optional comparison libraries (may fail to install)
# AutoClean>=1.1  # Inactive — install manually if needed
# datacleaner     # Abandoned — install manually if needed
