"""Minimal setup.py for ASV virtualenv detection."""

from setuptools import setup

setup(
    name="freshdata-benchmarks",
    version="0.1.0",
    description="ASV benchmark suite for FreshData",
    python_requires=">=3.9",
)
